# orchard-rsvp

Live: https://orchard-rsvp-two.vercel.app

A static site. Open `index.html` in a browser, or serve the folder locally:

```bash
python3 -m http.server 8000
```

Deploys automatically from `main` via Vercel.
